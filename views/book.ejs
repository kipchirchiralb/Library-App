<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Library App</title>
    <style>
        .flex{display: flex;} .flex>div:first-child{flex:1} .flex>div:last-child{flex:1} .flex>div img{width: 100%;}
    </style>
</head>
<body>
    <%- include('header.ejs') %>
    <main>
        <section id="hero">
            <h1><%= locals.book.title ></h1>  
            <div class="flex">
                <div>
                    <img src="/images/cover1.png" alt="Cover Page:  <%= locals.book.title %> ">
                </div>
                <div>
                    <h2> <%= locals.book.FullName %> </h2>
                    <p> <%= locals.book.publication %> </p>
                    <a href="/borrow?isbn=<%= locals.book.isbn %>"></a>
                    <p> <%= locals.book.synopsis %> </p>
                </div>
            </div>         
        </section>

    </main>
    <footer>
        <p> LibApp &copy; <%= new Date().getFullYear() %> . All rights Reserved. </p>
    </footer>
</body>
</html>